# VFX Corpus → CkParticles Cookbook — PROGRESS

**Directive (2026-07-12, user):** finish autonomously without input: exporter → full corpus export →
recreate representative VFX as CkParticles behaviors + gym stations. No commits/pushes until asked.
**Spec:** `docs/superpowers/specs/2026-07-12-vfx-corpus-exporter-design.md` (read it first — includes
implementation findings + phase definitions).

## State ledger (update as phases close)

| Phase | Status | Evidence |
|---|---|---|
| 0 Preconditions | DONE | Packs copied to `Content/` (4,108 uassets: sA_GiganticPack 2436, Shooter_VFXPack 454, M5VFXVOL2 427, Turrets_Pack 316, StarterContent 264, Vefects 211); git-excluded via `D:/Repos/CkPlugins/.git/info/exclude` (worktree common dir); editor closed; submodules clean except sibling-session artifacts (CkAuto, GitLink mods, CkWatermark generated dir, CONTINUATION_RelayClientRegistration/) — all untouched |
| 0 Baseline build | CONTAMINATED — accepted | The pre-edit build picked up in-flight A1 files (started before edits, compiled during). Single error: my `NiagaraNodeParameterMapSet.h` include (Private header — fixed via UEdGraphNode + class-name check). Everything else compiled. Accepted baseline: commit `6cd40f1` (prior session green, SM 57/57) + this run's partial evidence. Post-edit full build is the real gate |
| A1 Exporter implementation | CODE WRITTEN, review in flight | 12 files in Plugins/CkFoundation/Source/CkAssetExporter/: NiagaraExporter rewritten (override-pin harvest via UNiagaraNodeParameterMapSet, curve DIs via GetCurveData, enum display names, user-param values, det/bounds, material paths, sort modes); NEW CascadeExporter (reflection dump + distribution rasterizer), MaterialExporter (props+params+expression histogram), TextureExporter (source→PNG capped 1024), VfxCorpusExporter (registry sweep by class, dedup, index.json, wipes corpus root per run), automation test `Ck.AssetExporter.ExportVfxCorpus`, Cascade AssetAction, console command `Ck.AssetExporter.ExportVfxCorpus`, Build.cs +ImageCore. All engine APIs verified against D:/Repos/UnrealEngineAngelscript. Review workflow wf_8e947a5a-d01. Deviation from spec: no ExporterTab button (console command instead); texture flipbookGrid dropped (derivable from recipe subImage) |
| A1 Adversarial review | DONE — 9 confirmed findings, all fixed | Review workflow wf_8e947a5a-d01 (15 agents, 3 lenses + refuting verifiers). Fixed: (1) LNK blocker — UNiagaraNodeInput accessors are MinimalAPI-unexported → reflection reads via FindFProperty (`Get_NodeObjectProperty`); (2) 5-arg GetUsedTextures is a UE_DEPRECATED(5.7) final no-op → new TOptional overload; (3) BIGGEST: nested dynamic-input overrides (incl. curve DIs) were harvested but never emitted — added `DoExpandDynamicInputOverrides` (recursive re-association via entry 'id') + stage-level `unattachedOverrides` so nothing drops silently; (4) pack-level output dirs collide for same-named assets → dirs now mirror full package folder chain (`DoGet_OutputSubDir`); (5) packagePath field inconsistent across exporters → all package names now, exporterVersion=2; (6) never-compiled gate (A2 addresses); (7) test was EditorContext|ProductFilter + hard-fail on empty project → StressFilter + zero-assets=skip; (8) test moved to `Tests/Test_VfxCorpusExporter.cpp` per repo convention; (9) include order. Bonus: Cascade text serializer guards for no-LOD emitters. Earlier: MapSet Private header → UEdGraphNode + class-name check; fmt `{{}}` escapes → `[{}]` |
| A2 Build gate | GREEN | `Build-Editor-A2.log`, Development, CkAssetExporter relinked 01:41; reflection workaround linked (no LNK2019) |
| A3 Full corpus export + validation | **DONE — all gates pass** | Test `Ck.AssetExporter.ExportVfxCorpus` green in **2m08s** (`BuildTest-VfxCorpus.log`): **1020 systems (902 niagara / 118 cascade), 604 materials, 329 textures — 0 failures**. Counts on disk == index. 897 recipes contain rasterized curves (10,533 curve instances), 0 unattached overrides, enum display names + RIP values verified in NS_BasicAttack recipe. 3 PNGs visually verified (6×6 smoke flipbook, lightning slash strips, impact star). Stale Content/Vefects exports deleted. NOTE: filename-prefix estimates were wrong; class-based discovery is truth (902 vs ~656 est) |
| B1 Archetype classification | DONE — 1020/1020 | wf_f3382d1c-381, 111 s. Histogram in `analysis/archetypes.json` + per-system labels in `analysis/labels_*.jsonl`. Top: fire 257, impact 131, explosion 82, muzzle 68, trail 67, smoke 60, aura 55, projectile 51, aoe 48 |
| B2 Recreation slate + deep extraction | SLATE PICKED; extraction RUNNING | 8 new behaviors (ids 9–16): MuzzleFlash, ImpactBurst, Tracer, SmokePlume, SparksBurst, GroundRing, LightningStrike, AuraSwirl — no new procedural textures needed for v1 (existing Glow/Flare/Smoke/Electric/Streak/Ring suffice). Extraction workflow wf_9cec37ff-4e9: one translation sheet per target from named marketplace exemplars |
| C CkParticles behaviors | CODE DONE, build GREEN (`Build-Editor-C3.log`), **PARITY CLEAN** (agent verified all 8 pairs line-by-line: salts, branches, constants, DI arg order, dispatch, DependentShaderFiles). Follow-ups (aesthetic only, both sides identical): MuzzleFlash/AuraSwirl use salt 7 (reserved for default size — harmless, they overwrite Size); LightningStrike Jag salts 4..9 collide with salts 5/9 at bands 1/5 | 8 behaviors ids 9–16 (MuzzleFlash, ImpactBurst, Tracer, SmokePlume, SparksBurst, GroundRing, LightningStrike, AuraSwirl): `Shaders/CkParticles/Behaviors/Behavior_*.ush` + bit-parallel CPU cases in `CkParticles_DataInterface.cpp` (+Saturate/Frac/Step/SmoothStep helpers) + dispatch in `CkParticles_Behaviors.ush` + `DependentShaderFiles` + Utils doc comment. Numbers grounded in translation sheets. One-shot archetypes use internal `frac(Age/Cycle + phase)` replay (template is continuous-spawn; a burst-template variant would make true one-shots — follow-up). No new textures needed (existing 6 procedurals) |
| D Particles gym + autotest | **DONE — gate GREEN** | 4 AS files in `Plugins/CkTests/Script/CkParticles/` + registry line. First run failed: AS `WorldContext` params are auto-bound (drop the `this` arg on `Spawn_BehaviorAtLocation`). Re-run green (`Test-Particles.log`): `Project.Functional Tests...Ck_AutoTest_Particles_SpawnAllBehaviors` Result={Success} under real RHI (`--no-nullrhi`), 0 AS errors, 0 Niagara compile issues — all 17 behaviors spawn live components. Gym visual pass remains [EDITOR-VERIFY] |
| E Docs + memory + report | DONE | CkParticles CLAUDE.md (behavior roster 0–16 + real gym section), CkAssetExporter Claude.md/CLAUDE.md (corpus pipeline + engine-API gotchas), session memory `vfx-corpus-cookbook-campaign`. Commits withheld per session policy — every gate green, trees ready to commit |
| **M (round 2) — fidelity evolution** | CODE DONE, gates running | User verdict on round 1: "majority of effects unimpressive… the swipe uses a flat sweep mesh and the UV coords make the texture move" + license to replicate meshes/materials/textures. Corpus-quantified diagnosis (digest over 902 Niagara systems): 44% mesh renderers, 91% burst spawn, 52% Dynamic Material Parameters, 42% SubUV — template had none. Shipped: **M1** StaticMesh exporter (OBJ+stats) + corpus `meshes/` chain; **M2** DI contract v2 (StageOutput += Scale/Orientation-quat/Dynamic-float4/Rotation/MeshIndex/VisTag, GPU+CPU lockstep, quat helpers both sides); **M3** procedural carrier meshes SM_CkParticles_{Sweep,Tube,Shell,Disc} (MeshDescription-built), masters M_CkParticles_{SweepErode,SoftSmoke,FresnelShell} (DynamicParameter x=dissolve y=distort z=pan w=boost, zero defaults), textures {SweepStreak,TileNoise}; **M4** template v2 (4 tagged renderers: camera/velocity/smoke sprites + MeshIndex-carrier mesh renderer; `PS_CkParticles_Template_Burst` via factory-pattern module adds + NIAGARA_API RIP-name writes — engine facts verified incl. missing-VisTag⇒renders-in-ALL-renderers, so every behavior writes VisTag), Utils burst routing table, env-gated `Ck.Particles.RebuildTemplateAssets`; **M5** Slash(7) = full Vefects NS_BasicAttack swipe replication (oriented sweep meshes + pan/dissolve dyn-curves + sparkle ring + wind ghost), Beam→Tube, Nova→Shell+Disc, 9-16 retargeted to streak/smoke/mesh renderers, burst behaviors on real Age. Engine-facts agent + RIP format verified from NiagaraEmitterFactoryNew.cpp:133-161 |
| **M6 gates** | **ALL GREEN** | Build green (`Build-Editor-M6b/c/d.log`; 2 TObjectPtr auto* fixes). Asset regen green x2 (`Test-RebuildTemplates2.log` Result={Success}; editor log: 8/8 textures, 3/3 masters, 4/4 meshes, behavior module **YES on BOTH templates**; 26 uassets on disk). **Real bug caught by the real-RHI gate:** DynamicParameter by-name connects silently missed — `ConnectMaterialExpressions` matches the RAW `Outputs` array (MaterialEditingLibrary.cpp:98) which still held CDO "Param1..4" names; SmoothStep/Add degraded to const fallbacks, AppendVector hard-failed SweepErode for SM6 → fix: force `Dyn->GetOutputs()` sync after setting ParamNames. Autotest `Ck_AutoTest_Particles_SpawnAllBehaviors` Result={Success} under `--no-nullrhi` (`Test-Particles-M6b.log`), zero material/Niagara errors. Corpus v3 (`BuildTest-VfxCorpus-M6.log`): 1020 systems / 604 materials / 329 textures / **251 carrier meshes — 0 failed anywhere**. Truth-check `meshes/Vefects/.../SM_VFX_Slash01.{json,obj}`: flat XY arc band (Z thickness 0.1), full 0-1 UVs, single DisAdd-material section — validates the procedural Sweep construction; theirs spans ~250° at r≈150 vs ours 140° at r=100 (tuning note). Gym untouched (spawn API unchanged; burst routing internal). Visual pass remains [EDITOR-VERIFY] |

## Follow-ups (next session candidates)

1. **Cookbook skill** — distill corpus + translation sheets into a `ck-vfx-cookbook` skill (archetype → recipe → behavior mapping) so any session answers VFX requests without re-analysis.
2. Visual-pass tuning: sweep arc span (SM_VFX_Slash01 spans ~250° at r≈150 vs our 140° at r=100), HDR intensities, dyn-param dissolve/pan ranges per behavior.
3. Remaining renderer evolutions: ribbon renderer option, SubUV flipbook support. (Velocity-aligned stretch and burst template SHIPPED in round 2.)
4. Burst template refinements: per-behavior burst count (fixed 96 today; surplus hidden), Loop Behavior=Once variant for true fire-and-forget gameplay spawns (today: infinite ~1.2 s loops, which is what the gym wants).
5. Aesthetic salt cleanups (MuzzleFlash/AuraSwirl salt 7; LightningStrike band salts 4..9 overlap 5/9).
6. MuzzleFlash (9) stays continuous+cycled by design (automatic-fire read); revisit if single-shot muzzle is requested.

## Phase C scoping (verified against code 2026-07-12)

- `FCkParticles_StageOutput` = Position, Velocity, Color (HDR ok, additive), **Size (float2)** — size-over-life IS expressible. No rotation output; stretch comes from x≠y size + renderer facing.
- Behavior recipe: `Shaders/CkParticles/Behaviors/Behavior_<Name>.ush` (~20 lines, self-driving from Age/Seed via `CkParticles_Rand/RandDir/NormalizedAge`, bit-identical 24-bit hash both sides) + case in `NDICkParticlesLocal::ExecuteStage_CPU` (CkParticles_DataInterface.cpp:94 switch) + `#include`+branch in `CkParticles_Behaviors.ush` + entry in `DependentShaderFiles` (DataInterface.cpp:20).
- Existing ids: Gravity=0, Swirl=1, Explosion=2, Fire=3, Fireworks=4, Galaxy=5, Beam=6, Slash=7, Nova=8. Next id = 9.
- Spawn: `UCk_Utils_Particles_UE::Spawn_BehaviorAtLocation(ctx, id, loc, rot, scale, TextureName)` — texture look = master material `BaseTexture` swap by name.
- Corpus test invocation traps (both hit + fixed 2026-07-12): (a) toolbox caches the test list — pass `--discover-fresh` after adding a test; (b) **StressFilter tests are invisible to the entire automation commandline** — the default "Standard" filter is Smoke|Engine|Product|Perf (engine `AutomationCommandline.cpp`), so ListAllTests/RunTests never see Stress tests and no toolbox flag changes the tier. The corpus test is therefore ProductFilter + env-gate: set `CK_VFX_CORPUS_EXPORT=1` before `--test --test-pattern ExportVfxCorpus`; without it the test is an instant skip.

## Key facts a fresh session must not re-derive

- CkPlugins_Other is a **linked git worktree** of `D:/Repos/CkPlugins` — `info/exclude` lives in the common gitdir.
- Marketplace source: `D:\Repos\CkMarketplaceTestbed\Content` (stock UE 5.7 project). Never commit copied packs.
- Existing exporter: `FCk_NiagaraExporter` (CkAssetExporter) walks stacks + RIP constants + renderers.
  Its blind spots (the whole point of A1): Override ParameterMapSet pins (dynamic inputs/curves/DI/links),
  enum display names, material/texture depth, batch mode. Details in spec "Implementation findings".
- Curve extraction path: `UNiagaraDataInterfaceCurveBase::GetCurveData()`; DI instances hang off
  `UNiagaraNodeInput::DataInterface` linked to override pins (dotted names flatten nesting).
- Stale prior-format exports at `Content/Vefects/Anime_VFX/Shared/Skills/NS_*.{json,txt}` — delete in A3.
- Build/test ONLY via `./CkAuto/UnrealToolbox.exe` (see /build-test skill). Editor lock probe first.
- CkParticles module doctrine (`Plugins/CkFoundation/Source/CkParticles/CLAUDE.md`): behaviors are
  .ush + CPU mirror in lockstep; add to `DependentShaderFiles`; gym at `Plugins/CkTests/Script/CkParticles/`.
